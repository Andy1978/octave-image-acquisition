Imported Win10.ova into VirtualBox, boot + login (Passw0rd!)

- Dann das ganze Verzeichnis /home/andy/josoansi.de/git/octave-dev/octave-packages/octave-image-acquisition
  in VirtualBox als "Gemeinsamer Ordner" verknüpft (Automatisch einbinden, Permanent erzeugen).

- Aus Debian heraus Download von
  https://ftpmirror.gnu.org/octave/windows/octave-9.3.0-w64-installer.exe
  in den Shared Ordner gelegt.
  (Setup direkt von Z: ging nicht, ich musste den Installer zuerst auf den Desktop kopieren)
  -> "Install just for me"

This is all you need to compile the media foundation wrapper.

In der VM kann man aus dem Startmenü eine "Bash Shell" starten, da ist octave, g++ usw. bereits im Pfad.
